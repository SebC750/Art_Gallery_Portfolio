class artwork{
    art(title, description, artworkLink, dateCreated){
         this.title = title;
         this.description = description;
         this.artworkLink = artworkLink;
         this.dateCreated = dateCreated;
    }

    //---------------------------------------------------//
    // Database Get queries for handling artworks. These get requests are just meant to retrieve and display the data. Other CRUD operations are done in my custom CMS (WIP).
    //---------------------------------------------------//
    getAll(){
       
    }
    getArtById(artId){

    }
}
